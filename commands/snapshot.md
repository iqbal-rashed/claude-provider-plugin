---
description: Save current settings.json as a named profile (example: /provider:snapshot kimi)
---

Use "$ARGUMENTS" as the profile name.

Call MCP tool `provider.snapshot_current` with:
- profile: "$ARGUMENTS"

Print the result.
