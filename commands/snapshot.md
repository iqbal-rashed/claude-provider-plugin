---
description: Save current settings.json as a named profile (example: /claude-switcher:snapshot kimi)
---

Use "$ARGUMENTS" as the profile name.

Call MCP tool `claude-switcher.snapshot_current` with:
- profile: "$ARGUMENTS"

Print the result.
